# Zed Unity Package moved

This standalone repository is no longer the canonical home for the Unity package.

Use the monorepo instead:

```text
https://github.com/GameBayoumy/zed-unity
```

Current Unity Package Manager Git URL:

```text
https://github.com/GameBayoumy/zed-unity.git?path=/packages/com.gamebayoumy.zed-unity
```

OpenUPM package:

```sh
openupm add com.gamebayoumy.zed-unity
```

Package path in the monorepo:

```text
packages/com.gamebayoumy.zed-unity
```

This old repository should be treated as archived/read-only.
